import type { NextPage } from "next";
// Styles
import styles from "../styles/Home.module.scss";

const Home: NextPage = () => {
  return (
    <div>
      <main className={styles.main}>
        <h1>Portfolio</h1>
      </main>
    </div>
  );
};

export default Home;
